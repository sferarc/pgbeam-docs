---
title: Add a custom domain
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Registers a new custom domain for the project. Returns DNS
          verification instructions. Requires a Scale or Enterprise plan.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Registers a new custom domain for the project. Returns DNS verification instructions. Requires a Scale or Enterprise plan.

<Callout type="warn" title="Feature flag: custom-domains">
  This endpoint is currently under a feature flag and available only to select testers.
  It may change or be removed before general availability.
</Callout>

## At a glance

| Item | Value |
| --- | --- |
| Method | `POST` |
| Path | `/v1/projects/{project_id}/domains` |
| Auth | `Authorization: Bearer <token-or-api-key>` |

## Quick request

Use this as a starting point. Replace the placeholder IDs, set `PGBEAM_TOKEN`, and create `body.json` from the schema below.

```bash
curl -X POST \
  "$PGBEAM_API_URL/v1/projects/<project_id>/domains" \
  -H "Authorization: Bearer $PGBEAM_TOKEN" \
  -H "Content-Type: application/json" \
  --data @body.json
```

<APIPage document={"../../../backend/openapi/bundles/public.yaml"} operations={[{"path":"/v1/projects/{project_id}/domains","method":"post"}]} />
