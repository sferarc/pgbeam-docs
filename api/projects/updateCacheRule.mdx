---
title: Update cache rule
full: true
_openapi:
  method: PUT
  toc: []
  structuredData:
    headings: []
    contents:
      - content: |
          Enable or disable caching for a specific query shape, with optional
          TTL and SWR overrides. Requires the query to exist in the cache rules.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Enable or disable caching for a specific query shape, with optional
TTL and SWR overrides. Requires the query to exist in the cache rules.

## At a glance

| Item   | Value                                                                        |
| ------ | ---------------------------------------------------------------------------- |
| Method | `PUT`                                                                        |
| Path   | `/v1/projects/{project_id}/databases/{database_id}/cache-rules/{query_hash}` |
| Auth   | `Authorization: Bearer <token-or-api-key>`                                   |

## Quick request

Use this as a starting point. Replace the placeholder IDs, set `PGBEAM_TOKEN`, and create `body.json` from the schema below.

```bash
curl -X PUT \
  "$PGBEAM_API_URL/v1/projects/<project_id>/databases/<database_id>/cache-rules/<query_hash>" \
  -H "Authorization: Bearer $PGBEAM_TOKEN" \
  -H "Content-Type: application/json" \
  --data @body.json
```

<APIPage document={"../../../backend/openapi/bundles/public.yaml"} operations={[{"path":"/v1/projects/{project_id}/databases/{database_id}/cache-rules/{query_hash}","method":"put"}]} />
