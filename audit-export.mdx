---
title: Audit Export
icon: Send
description:
  Stream audit events to your own systems. HMAC-signed webhooks for any endpoint,
  plus native formats for Splunk HEC, Datadog, and Elastic.
---

Audit export pushes PgBeam events to your systems as they happen. Point it at a
webhook endpoint and PgBeam delivers a signed JSON payload for each event. Point
it at a SIEM and PgBeam formats the events the way that SIEM expects. The
[audit log](/docs/audit-log) keeps the full history for querying; export is for
getting events out in real time.

## Event types

| Event                | Fires when                                                                        |
| -------------------- | --------------------------------------------------------------------------------- |
| `query_blocked`      | A statement is rejected by policy (allowlist, read-only, etc.).                   |
| `budget_exhausted`   | A credential hits its query or row budget.                                        |
| `kill_switch`        | A credential or project kill-switch is tripped.                                   |
| `masked`             | A result is returned with one or more masked columns.                             |
| `migration_flagged`  | A DDL statement is flagged by the [safe-migration](/docs/safe-migrations) linter. |
| `approval_requested` | A write or DDL is held for [approval](/docs/approvals).                           |
| `anomaly_alert`      | [Anomaly detection](/docs/anomaly-detection) raises an alert.                     |

For each event's full payload shape, see [webhook events](/docs/webhook-events).

## Create a webhook endpoint

<Tabs items={["CLI", "Dashboard", "API"]}>
  <Tab value="CLI">
    ```bash
    pgbeam webhooks create https://hooks.example.com/pgbeam \
      --event query_blocked,kill_switch,anomaly_alert
    ```
  </Tab>

<Tab value="Dashboard">
  Open **Webhooks**, add an endpoint URL, pick the events to send, and copy the
  signing secret. Use **Send test event** to verify your receiver before you
  rely on it.
</Tab>

  <Tab value="API">
    ```json title="Create endpoint"
    {
      "url": "https://hooks.example.com/pgbeam",
      "event_types": ["query_blocked", "kill_switch", "anomaly_alert"],
      "format": "json"
    }
    ```
  </Tab>
</Tabs>

You set the signing secret when you create the endpoint. It is write-only:
PgBeam stores it to sign deliveries and never returns it, so keep your own copy.
The same secret verifies every delivery.

## Webhook payload

Each delivery is a JSON body with the event, the project, and the event-specific
detail under `data`. The [webhook events](/docs/webhook-events) page documents the
`data` fields for every event type.

```json title="query_blocked delivery"
{
  "id": "whd_2a9f1c",
  "type": "query_blocked",
  "project_id": "prj_abc",
  "occurred_at": "2026-06-13T09:24:11.512Z",
  "data": {
    "audit_id": "aud_9f2c",
    "credential_id": "agent_4f2c",
    "region": "us-east-1",
    "event": "blocked",
    "sql": "DELETE FROM users",
    "reason": "policy is read-only: DELETE is not allowed"
  }
}
```

PgBeam signs every native (`json`) and `elastic` delivery. The signature is sent
in the `X-PgBeam-Signature` header as `sha256=<hex>`, where the value is the
HMAC-SHA-256 of the raw request body keyed with your signing secret. Each delivery
also carries `X-PgBeam-Event`, `X-PgBeam-Event-Id`, `X-PgBeam-Timestamp` (unix
seconds), and `X-PgBeam-Delivery` (a per-attempt id for de-duplication).

```ts title="Verify a delivery (Node.js)"
import { createHmac, timingSafeEqual } from "node:crypto";

export function verify(
  rawBody: string,
  signatureHeader: string,
  secret: string,
): boolean {
  const expected =
    "sha256=" + createHmac("sha256", secret).update(rawBody).digest("hex");

  const a = Buffer.from(signatureHeader);
  const b = Buffer.from(expected);
  return a.length === b.length && timingSafeEqual(a, b);
}
```

Verify against the raw request body, before any JSON parsing reserializes it.
Compare with a constant-time function. To defend against replays, reject
deliveries whose `X-PgBeam-Timestamp` is far from the current time.

<Callout type="info" title="SIEM authentication">
  Splunk HEC and Datadog deliveries are not HMAC-signed. They authenticate with
  the destination's own token instead: Splunk uses an `Authorization: Splunk
  <token>` header and Datadog uses `DD-API-KEY`. Set that token as the endpoint's
  secret.
</Callout>

<Callout type="info" title="Delivery and retries">
  PgBeam expects a `2xx` within a few seconds. A non-`2xx` or a timeout is
  retried with exponential backoff. Make your receiver idempotent by keying on
  the event `id`, which is stable across retries.
</Callout>

## SIEM formats

For a SIEM, set the endpoint's `format` and PgBeam shapes each event the way that
product ingests it. The signing and retry behavior is the same.

| Format       | Sends                                                                         |
| ------------ | ----------------------------------------------------------------------------- |
| `splunk_hec` | Splunk HTTP Event Collector (HEC) envelope. Set your HEC token as the secret. |
| `datadog`    | Datadog Logs intake payload with `ddsource: pgbeam` and event tags.           |
| `elastic`    | Elastic / OpenSearch JSON documents with an ECS-style shape.                  |

For `splunk_hec` and `datadog`, the endpoint secret is the destination's own
token (Splunk sends it as `Authorization: Splunk <token>`, Datadog as
`DD-API-KEY`) rather than an HMAC signature.

```bash title="Export to Splunk HEC"
pgbeam webhooks create https://splunk.example.com:8088/services/collector \
  --format splunk_hec \
  --secret "$SPLUNK_HEC_TOKEN" \
  --event query_blocked,budget_exhausted,kill_switch,anomaly_alert
```

## Related

- [Webhook events](/docs/webhook-events): every event type and its payload shape.
- [Audit log](/docs/audit-log): the queryable history these events come from.
- [Anomaly detection](/docs/anomaly-detection): source of `anomaly_alert` events.
- [Approvals](/docs/approvals): source of `approval_requested` events.
- [Safe migrations](/docs/safe-migrations): source of `migration_flagged` events.
