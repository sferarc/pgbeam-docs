---
title: ListOrgInvitations
description: List invitations
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Lists invitations to join the organization and where each has got to.

## Usage

```go
result, err := client.Account.ListOrgInvitations(ctx, "org_abc123", nil)
```

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| ctx | `context.Context` | Yes | Request context |
| orgID | `string` | Yes | Unique organization identifier. |
| params | `*pgbeam.ListOrgInvitationsParams` | No | Query parameters |
| params.Status | `string` | No | Filter to a single status. Omit to list every invitation. |
| params.PageSize | `int` | No | Maximum number of items to return (1-100, default 20). |
| params.PageToken | `string` | No | Opaque token for cursor-based pagination. |

## Response

`(*pgbeam.ListOrgInvitationsResponse, error)`: a page of invitations.

## Example

```go
import pgbeam "go.pgbeam.com/sdk"

client := pgbeam.NewClient(&pgbeam.ClientOptions{
  APIKey: "pgb_your_api_key",
})

result, err := client.Account.ListOrgInvitations(ctx, "org_abc123", &pgbeam.ListOrgInvitationsParams{
    Status: "open",
    PageSize: 20,
    PageToken: "...",
  })
if err != nil {
  log.Fatal(err)
}
fmt.Println(result)
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
| 429    | Rate limited. Try again later.                |
