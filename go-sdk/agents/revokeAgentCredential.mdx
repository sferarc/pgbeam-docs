---
title: RevokeAgentCredential
description: Revoke an agent credential
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Permanently revokes the credential and drops any live connections.

## Usage

```go
err := client.Agents.RevokeAgentCredential(ctx, "prj_xxx", "agt_xxx")
```

## Parameters

| Parameter | Type              | Required | Description                                                  |
| --------- | ----------------- | -------- | ------------------------------------------------------------ |
| ctx       | `context.Context` | Yes      | Request context                                              |
| projectID | `string`          | Yes      | Unique project identifier (prefixed, e.g. prj_xxx).          |
| agentID   | `string`          | Yes      | Unique agent credential identifier (prefixed, e.g. agt_xxx). |

## Response

`error` — returns nil on success.

## Example

```go
import pgbeam "go.pgbeam.com/sdk"

client := pgbeam.NewClient(&pgbeam.ClientOptions{
  APIKey: "pgb_your_api_key",
})

err := client.Agents.RevokeAgentCredential(ctx, "prj_xxx", "agt_xxx")
if err != nil {
  log.Fatal(err)
}
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
