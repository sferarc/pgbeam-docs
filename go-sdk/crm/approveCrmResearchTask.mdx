---
title: ApproveCrmResearchTask
description: Approve a research task
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Approves a task that is awaiting approval (an irreversible action). The task flips to pending and is then run by the worker.

## Usage

```go
result, err := client.Crm.ApproveCrmResearchTask(ctx, "org_abc123", "ctk_xxx")
```

## Parameters

| Parameter | Type              | Required | Description                                                   |
| --------- | ----------------- | -------- | ------------------------------------------------------------- |
| ctx       | `context.Context` | Yes      | Request context                                               |
| orgID     | `string`          | Yes      | Unique organization identifier.                               |
| taskID    | `string`          | Yes      | Unique CRM research task identifier (prefixed, e.g. ctk_xxx). |

## Response

`(*pgbeam.ResearchTask, error)`: the approved task.

## Example

```go
import pgbeam "go.pgbeam.com/sdk"

client := pgbeam.NewClient(&pgbeam.ClientOptions{
  APIKey: "pgb_your_api_key",
})

result, err := client.Crm.ApproveCrmResearchTask(ctx, "org_abc123", "ctk_xxx")
if err != nil {
  log.Fatal(err)
}
fmt.Println(result)
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
