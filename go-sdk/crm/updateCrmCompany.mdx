---
title: UpdateCrmCompany
description: Update a company
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Update a company.

## Usage

```go
result, err := client.Crm.UpdateCrmCompany(ctx, "org_abc123", "cmp_xxx", pgbeam.CompanyInput{
    Name: "Acme Corp",
  })
```

## Parameters

| Parameter       | Type                  | Required | Description                                             |
| --------------- | --------------------- | -------- | ------------------------------------------------------- |
| ctx             | `context.Context`     | Yes      | Request context                                         |
| orgID           | `string`              | Yes      | Unique organization identifier.                         |
| companyID       | `string`              | Yes      | Unique CRM company identifier (prefixed, e.g. cmp_xxx). |
| req             | `pgbeam.CompanyInput` | Yes      | Request body                                            |
| req.Name        | `string`              | Yes      | Company name.                                           |
| req.Domain      | `*string`             | No       | Primary web domain.                                     |
| req.Industry    | `*string`             | No       | Industry or sector.                                     |
| req.Location    | `*string`             | No       | Primary location.                                       |
| req.LogoURL     | `*string`             | No       | URL of the company logo.                                |
| req.Description | `*string`             | No       | Free-form description or research notes.                |

## Response

`(*pgbeam.Company, error)`: updated company.

## Example

```go
import pgbeam "go.pgbeam.com/sdk"

client := pgbeam.NewClient(&pgbeam.ClientOptions{
  APIKey: "pgb_your_api_key",
})

result, err := client.Crm.UpdateCrmCompany(ctx, "org_abc123", "cmp_xxx", pgbeam.CompanyInput{
    Name: "Acme Corp",
    Domain: "acme.com",
    Industry: "...",
    Location: "...",
    LogoURL: "...",
    Description: "...",
  })
if err != nil {
  log.Fatal(err)
}
fmt.Println(result)
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
