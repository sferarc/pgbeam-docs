---
title: UpdateCrmContact
description: Update a contact
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Update a contact.

## Usage

```go
result, err := client.Crm.UpdateCrmContact(ctx, "org_abc123", "cnt_xxx", pgbeam.ContactInput{
    Name: "...",
  })
```

## Parameters

| Parameter       | Type                  | Required | Description                                             |
| --------------- | --------------------- | -------- | ------------------------------------------------------- |
| ctx             | `context.Context`     | Yes      | Request context                                         |
| orgID           | `string`              | Yes      | Unique organization identifier.                         |
| contactID       | `string`              | Yes      | Unique CRM contact identifier (prefixed, e.g. cnt_xxx). |
| req             | `pgbeam.ContactInput` | Yes      | Request body                                            |
| req.CompanyID   | `*string`             | No       | Company to attach this contact to, if any.              |
| req.Name        | `string`              | Yes      | Contact full name.                                      |
| req.Email       | `*string`             | No       | Contact email address.                                  |
| req.Title       | `*string`             | No       | Job title or role.                                      |
| req.Phone       | `*string`             | No       | Phone number.                                           |
| req.LinkedinURL | `*string`             | No       | LinkedIn profile URL.                                   |
| req.Notes       | `*string`             | No       | Free-form notes about the contact.                      |

## Response

`(*pgbeam.Contact, error)`: updated contact.

## Example

```go
import pgbeam "go.pgbeam.com/sdk"

client := pgbeam.NewClient(&pgbeam.ClientOptions{
  APIKey: "pgb_your_api_key",
})

result, err := client.Crm.UpdateCrmContact(ctx, "org_abc123", "cnt_xxx", pgbeam.ContactInput{
    CompanyID: "...",
    Name: "...",
    Email: "...",
    Title: "...",
    Phone: "...",
    LinkedinURL: "...",
  })
if err != nil {
  log.Fatal(err)
}
fmt.Println(result)
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
