---
title: UpdateCrmDeal
description: Update a deal
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Update a deal.

## Usage

```go
result, err := client.Crm.UpdateCrmDeal(ctx, "org_abc123", "dl_xxx", pgbeam.DealInput{
    Title: "...",
  })
```

## Parameters

| Parameter            | Type                | Required | Description                                            |
| -------------------- | ------------------- | -------- | ------------------------------------------------------ |
| ctx                  | `context.Context`   | Yes      | Request context                                        |
| orgID                | `string`            | Yes      | Unique organization identifier.                        |
| dealID               | `string`            | Yes      | Unique CRM deal identifier (prefixed, e.g. dl_xxx).    |
| req                  | `pgbeam.DealInput`  | Yes      | Request body                                           |
| req.CompanyID        | `*string`           | No       | Company the deal is with, if any.                      |
| req.PrimaryContactID | `*string`           | No       | Primary contact on the deal, if any.                   |
| req.Title            | `string`            | Yes      | Deal title.                                            |
| req.Stage            | `*pgbeam.DealStage` | No       |                                                        |
| req.AmountCents      | `*int`              | No       | Deal value in the smallest currency unit (e.g. cents). |
| req.Currency         | `*string`           | No       | ISO 4217 currency code.                                |
| req.Owner            | `*string`           | No       | Deal owner (free-form, e.g. a user name or email).     |
| req.CloseDate        | `*string`           | No       | Expected or actual close date.                         |

## Response

`(*pgbeam.Deal, error)`: updated deal.

## Example

```go
import pgbeam "go.pgbeam.com/sdk"

client := pgbeam.NewClient(&pgbeam.ClientOptions{
  APIKey: "pgb_your_api_key",
})

result, err := client.Crm.UpdateCrmDeal(ctx, "org_abc123", "dl_xxx", pgbeam.DealInput{
    CompanyID: "...",
    PrimaryContactID: "...",
    Title: "...",
    Stage: pgbeam.DealStage{ /* ... */ },
    AmountCents: 0,
    Currency: "USD",
  })
if err != nil {
  log.Fatal(err)
}
fmt.Println(result)
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
