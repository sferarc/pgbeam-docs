---
title: ReleaseDemoCredential
description: Release a demo credential
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Revokes a demo credential before its TTL runs out and returns its slot to the global ceiling. Callers that finish early should do this so the next evaluating agent is not turned away by a session nobody is using. The MCP token issued with the credential proves the caller holds it. Releasing an already-expired or already-released session succeeds with released=false.

## Usage

```go
result, err := client.Demo.ReleaseDemoCredential(ctx, "...", pgbeam.ReleaseDemoCredentialRequest{
    McpToken: "...",
  })
```

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| ctx | `context.Context` | Yes | Request context |
| sessionID | `string` | Yes | The demo session to release, as returned when it was issued. |
| req | `pgbeam.ReleaseDemoCredentialRequest` | Yes | Request body |
| req.McpToken | `string` | Yes | The mcp_token returned when the credential was issued. |

## Response

`(*pgbeam.DemoReleaseResult, error)`: the session was released, or was already gone.

## Example

```go
import pgbeam "go.pgbeam.com/sdk"

client := pgbeam.NewClient(&pgbeam.ClientOptions{
  APIKey: "pgb_your_api_key",
})

result, err := client.Demo.ReleaseDemoCredential(ctx, "...", pgbeam.ReleaseDemoCredentialRequest{
    McpToken: "...",
  })
if err != nil {
  log.Fatal(err)
}
fmt.Println(result)
```

## Errors

| Status | Description                        |
| ------ | ---------------------------------- |
| 400    | Invalid request parameters.        |
| 401    | Missing or invalid authentication. |
| 404    | Resource not found.                |
| 429    | Rate limited. Try again later.     |
| 503    | Service temporarily unavailable.   |
