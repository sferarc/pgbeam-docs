---
title: ListReplicas
description: List replicas
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Lists a database's read replicas.

## Usage

```go
result, err := client.Projects.ListReplicas(ctx, "db_xxx", nil)
```

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| ctx | `context.Context` | Yes | Request context |
| databaseID | `string` | Yes | Unique database identifier (prefixed, e.g. db_xxx). |
| params | `*pgbeam.ListReplicasParams` | No | Query parameters |
| params.PageSize | `int` | No | Maximum number of items to return (1-100, default 20). |
| params.PageToken | `string` | No | Opaque token for cursor-based pagination. |

## Response

`(*pgbeam.ListReplicasResponse, error)`: list of replicas.

## Example

```go
import pgbeam "go.pgbeam.com/sdk"

client := pgbeam.NewClient(&pgbeam.ClientOptions{
  APIKey: "pgb_your_api_key",
})

result, err := client.Projects.ListReplicas(ctx, "db_xxx", &pgbeam.ListReplicasParams{
    PageSize: 20,
    PageToken: "...",
  })
if err != nil {
  log.Fatal(err)
}
fmt.Println(result)
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
| 429    | Rate limited. Try again later.                |
