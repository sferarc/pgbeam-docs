---
title: CreateSupportMessage
description: Add a message to a support case
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Adds a new message to an existing support case thread.

## Usage

```go
result, err := client.Support.CreateSupportMessage(ctx, "org_abc123", "sc_xxx", pgbeam.CreateSupportMessageRequest{
    Body: "...",
  })
```

## Parameters

| Parameter | Type                                 | Required | Description                                             |
| --------- | ------------------------------------ | -------- | ------------------------------------------------------- |
| ctx       | `context.Context`                    | Yes      | Request context                                         |
| orgID     | `string`                             | Yes      | Unique organization identifier.                         |
| caseID    | `string`                             | Yes      | Unique support case identifier (prefixed, e.g. sc_xxx). |
| req       | `pgbeam.CreateSupportMessageRequest` | Yes      | Request body                                            |
| req.Body  | `string`                             | Yes      | Message content.                                        |

## Response

`(*pgbeam.SupportMessage, error)`: message created.

## Example

```go
import pgbeam "go.pgbeam.com/sdk"

client := pgbeam.NewClient(&pgbeam.ClientOptions{
  APIKey: "pgb_your_api_key",
})

result, err := client.Support.CreateSupportMessage(ctx, "org_abc123", "sc_xxx", pgbeam.CreateSupportMessageRequest{
    Body: "...",
  })
if err != nil {
  log.Fatal(err)
}
fmt.Println(result)
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
