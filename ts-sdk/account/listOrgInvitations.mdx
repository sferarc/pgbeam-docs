---
title: listOrgInvitations
description: List invitations
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Lists invitations to join the organization and where each has got to.

## Usage

```typescript
const result = await api.account.listOrgInvitations({
  pathParams: { org_id: "org_abc123" },
});
```

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| pathParams.org_id | `string` | Yes | Unique organization identifier. |
| queryParams.status | `"pending" \| "accepted" \| "rejected" \| "canceled"` | No | Filter to a single status. Omit to list every invitation. |
| queryParams.page_size | `number` | No | Maximum number of items to return (1-100, default 20). |
| queryParams.page_token | `string` | No | Opaque token for cursor-based pagination. |

## Response

`Promise<ListOrgInvitationsResponse>`: a page of invitations.

## Example

```typescript
import { PgBeamClient } from "pgbeam";

const client = new PgBeamClient({
  token: "pbk_...",
  baseUrl: "https://api.pgbeam.com",
});

const result = await client.api.account.listOrgInvitations({
  pathParams: { org_id: "org_abc123" },
  queryParams: { status: "open", page_size: 20, page_token: "..." },
});
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
| 429    | Rate limited. Try again later.                |
