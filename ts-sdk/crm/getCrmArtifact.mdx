---
title: getCrmArtifact
description: Get an artifact
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Returns a single artifact by ID.

## Usage

```typescript
const result = await api.crm.getCrmArtifact({
  pathParams: { org_id: "org_abc123", artifact_id: "art_xxx" },
});
```

## Parameters

| Parameter              | Type     | Required | Description                                              |
| ---------------------- | -------- | -------- | -------------------------------------------------------- |
| pathParams.org_id      | `string` | Yes      | Unique organization identifier.                          |
| pathParams.artifact_id | `string` | Yes      | Unique CRM artifact identifier (prefixed, e.g. art_xxx). |

## Response

`Promise<Artifact>`: the artifact.

## Example

```typescript
import { PgBeamClient } from "pgbeam";

const client = new PgBeamClient({
  token: "pbk_...",
  baseUrl: "https://api.pgbeam.com",
});

const result = await client.api.crm.getCrmArtifact({
  pathParams: { org_id: "org_abc123", artifact_id: "art_xxx" },
});
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
