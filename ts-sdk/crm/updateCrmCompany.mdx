---
title: updateCrmCompany
description: Update a company
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Update a company.

## Usage

```typescript
const result = await api.crm.updateCrmCompany({
  pathParams: { org_id: "org_abc123", company_id: "cmp_xxx" },
  body: {
    name: "Acme Corp",
  },
});
```

## Parameters

| Parameter             | Type     | Required | Description                                             |
| --------------------- | -------- | -------- | ------------------------------------------------------- |
| pathParams.org_id     | `string` | Yes      | Unique organization identifier.                         |
| pathParams.company_id | `string` | Yes      | Unique CRM company identifier (prefixed, e.g. cmp_xxx). |
| body.name             | `string` | Yes      | Company name.                                           |
| body.domain           | `string` | No       | Primary web domain.                                     |
| body.industry         | `string` | No       | Industry or sector.                                     |
| body.location         | `string` | No       | Primary location.                                       |
| body.logo_url         | `string` | No       | URL of the company logo.                                |
| body.description      | `string` | No       | Free-form description or research notes.                |

## Response

`Promise<Company>`: updated company.

## Example

```typescript
import { PgBeamClient } from "pgbeam";

const client = new PgBeamClient({
  token: "pbk_...",
  baseUrl: "https://api.pgbeam.com",
});

const result = await client.api.crm.updateCrmCompany({
  pathParams: { org_id: "org_abc123", company_id: "cmp_xxx" },
  body: {
    name: "Acme Corp",
  },
});
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
