---
title: updateCrmDeal
description: Update a deal
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Update a deal.

## Usage

```typescript
const result = await api.crm.updateCrmDeal({
  pathParams: { org_id: "org_abc123", deal_id: "dl_xxx" },
  body: {
    title: "...",
  },
});
```

## Parameters

| Parameter               | Type        | Required | Description                                            |
| ----------------------- | ----------- | -------- | ------------------------------------------------------ |
| pathParams.org_id       | `string`    | Yes      | Unique organization identifier.                        |
| pathParams.deal_id      | `string`    | Yes      | Unique CRM deal identifier (prefixed, e.g. dl_xxx).    |
| body.company_id         | `string`    | No       | Company the deal is with, if any.                      |
| body.primary_contact_id | `string`    | No       | Primary contact on the deal, if any.                   |
| body.title              | `string`    | Yes      | Deal title.                                            |
| body.stage              | `DealStage` | No       |                                                        |
| body.amount_cents       | `number`    | No       | Deal value in the smallest currency unit (e.g. cents). |
| body.currency           | `string`    | No       | ISO 4217 currency code.                                |
| body.owner              | `string`    | No       | Deal owner (free-form, e.g. a user name or email).     |
| body.close_date         | `string`    | No       | Expected or actual close date.                         |

## Response

`Promise<Deal>`: updated deal.

## Example

```typescript
import { PgBeamClient } from "pgbeam";

const client = new PgBeamClient({
  token: "pbk_...",
  baseUrl: "https://api.pgbeam.com",
});

const result = await client.api.crm.updateCrmDeal({
  pathParams: { org_id: "org_abc123", deal_id: "dl_xxx" },
  body: {
    title: "...",
  },
});
```

## Errors

| Status | Description                                   |
| ------ | --------------------------------------------- |
| 400    | Invalid request parameters.                   |
| 401    | Missing or invalid authentication.            |
| 403    | Operation not allowed by current plan limits. |
| 404    | Resource not found.                           |
