---
title: getHealth
description: Health check
---

{/* Auto-generated from OpenAPI. Do not edit. Run `pnpm generate:sdk-docs` to regenerate. */}

Returns the health status of the API server.

## Usage

```typescript
const result = await api.platform.getHealth();
```

## Parameters

None.

## Response

`Promise<HealthResponse>` — service is healthy.

## Example

```typescript
import { PgBeamClient } from "pgbeam";

const client = new PgBeamClient({
  token: null,
  baseUrl: "https://api.pgbeam.com",
});

const result = await client.api.platform.getHealth();
```

## Errors

| Status | Description                    |
| ------ | ------------------------------ |
| 429    | Rate limited. Try again later. |
